# Pingo 待办事项

用于追踪 MVP 之后的功能、体验优化和发布工作。完成一项后勾选，并在必要时补充验证结果或相关说明。

状态约定：

- `[ ]` 待处理
- `[-]` 进行中
- `[x]` 已完成

## 当前最高优先级：可预测基础能力

- [x] 增加主进程确定性能力路由；命中内置能力后不再依赖模型决定是否调用工具。
- [x] 实现天气查询 MVP：
  - [x] 在设置中增加默认城市；用户只问“今天天气怎样”时使用默认城市。
  - [x] 支持从“上海今天天气”“明天深圳会下雨吗”等问题中提取城市和日期。
  - [x] 使用固定域名白名单调用地理编码和天气预报服务。
  - [x] 返回天气状况、当前温度、体感温度、最高/最低温、降雨概率和风速。
  - [x] 没有默认城市时固定询问城市，不让模型猜测位置。
  - [x] 城市无结果、服务超时或网络失败时返回明确且固定的错误提示。
- [x] 为天气路由增加回归测试：
  - [x] 天气意图在主进程路由中优先处理，不进入普通模型调用。
  - [x] 显式城市覆盖默认城市。
  - [x] 无城市、城市不存在、接口失败均有可预期结果。
  - [x] 普通知识和项目问题仍进入现有模型/只读工具链。
- [ ] 天气 MVP 稳定后，再评估时间、汇率和通用网页搜索等能力。

## 次优先级：动画宠物与交互升级

- [-] 规划并接入动画宠物素材
  - [ ] 收集透明 PNG 序列帧或 Sprite Sheet。
  - [ ] 统一单帧画布尺寸、角色中心和脚底锚点。
  - [ ] 覆盖 `idle`、`hover`、`thinking`、`working`、`listening`、`needs-action`、`success`、`error` 状态。
  - [ ] 为每组动画记录帧率、是否循环和结束后的状态。
- [ ] 将宠物动画与真实聊天、工具执行和权限等待状态联动。
- [ ] 增加悬停反馈和快捷控制。
- [ ] 增加 `Esc` 行为：取消语音、关闭卡片或收起面板。
- [ ] 完善拖动阈值，确保拖动不会误触发展开/收起。
- [ ] 实现收起态透明区域点击穿透，同时保留宠物和快捷按钮的交互。
- [ ] 升级悬浮任务卡：
  - [ ] 展示任务标题、当前阶段和状态。
  - [ ] 支持取消、重试、查看原因、打开结果和继续追问。
  - [ ] 支持权限请求中的“允许一次 / 始终允许 / 拒绝”。
  - [ ] 支持多个任务堆叠和切换。
- [ ] 增加语音按钮和监听状态反馈；未明确开启时不持续监听。
- [ ] 增加安静模式、完成后自动收起、悬停暂停收起和减少动态效果支持。

## 发布前工作

- [ ] 配置 Developer ID 代码签名。
- [ ] 完成 macOS 公证，并在干净环境或新用户账户中安装验证。
- [ ] 补充打包版首次启动、无密钥、断网、模型超时和权限异常的手工回归记录。
- [ ] 验证多显示器、屏幕缩放、显示器断开和重启恢复场景。
- [ ] 更新最终安装与配置说明，明确退出方式、托盘入口和配置文件权限。

## 后续功能候选

- [ ] 更换为 macOS Keychain 等系统安全存储，替代打包版 `.env` 配置。
- [ ] 长期记忆和可管理的历史会话。
- [ ] 语音输入与语音播报。
- [ ] 在用户确认后修改文件或生成补丁。
- [ ] 在严格白名单和确认机制下支持有限命令执行。
- [ ] 插件扩展机制或插件市场。
- [ ] 云端账号、同步和多设备配置。
- [ ] Windows / Linux 跨平台适配。

## 已完成：近期功能

- [x] 宠物右键菜单显示“关闭 Pingo”，点击后退出应用。
- [x] 透明、无边框、置顶窗口及拖动、边缘吸附和位置持久化。
- [x] 托盘显示、隐藏、设置和退出入口。
- [x] 聊天面板、流式回复、停止生成、清空会话和本地消息保存。
- [x] 项目目录授权及 `list_files`、`search_files`、`read_file` 只读工具。
- [x] IPC 安全边界、路径保护、敏感文件拦截和模型工具调用循环。
- [x] 设置页、全局快捷键、宠物缩放、透明度和开机启动配置。
- [x] lint、类型检查、自动化测试、生产构建和 macOS arm64 打包流程。

## 记录

- 2026-08-06：新增“可预测基础能力”主线；天气 MVP 采用确定性路由，不依赖模型自行选择工具。
- 2026-08-06：天气查询 MVP 已完成；支持默认城市、显式城市、固定结果和失败兜底。
- 2026-08-06：重新生成 macOS arm64 安装包，修复旧版 `Pingo.app` 未包含天气路由的问题。
- 2026-08-06：创建待办文档；MVP 主线完成，动画宠物交互升级和发布签名/公证仍待处理。
